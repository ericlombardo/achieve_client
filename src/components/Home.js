// Displays / route
import Quote from './Quote'
import NavBar from './NavBar'

const Home = () => {
  return (
    <div id="home">
      <Quote />
      <NavBar />
    </div>
  )
}

export default Home
